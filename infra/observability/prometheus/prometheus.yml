global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets:
          - "prometheus:9090"

  # Future Sprint note:
  # Add local service targets here once services expose Prometheus-compatible metrics.
  # Example:
  # - job_name: "api-gateway"
  #   static_configs:
  #     - targets:
  #         - "host.docker.internal:8080"
