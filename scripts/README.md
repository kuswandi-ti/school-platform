# Scripts

Project automation scripts live here.

Existing GitHub repository setup scripts are under `scripts/github`. Scripts must not contain real tokens, private keys, database passwords, production credentials, or production data.
