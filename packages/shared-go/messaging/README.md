# Messaging

Generic messaging interfaces for future RabbitMQ integration.

This package intentionally does not implement a RabbitMQ publisher or consumer yet. Concrete infrastructure can be added when multiple services need the same abstraction.
