# Logger

Generic `slog` setup helpers.

Use this package to create structured JSON loggers with consistent `service` and `environment` attributes. Do not log tokens, passwords, raw document content, or Confidential data.
