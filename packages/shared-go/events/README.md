# Events

Generic domain event envelope placeholder types.

Domain event names should follow `domain.entity.action_past_tense`, for example `finance.payment.verified`.

Do not put tokens, passwords, raw document content, or Confidential data in event payloads.
