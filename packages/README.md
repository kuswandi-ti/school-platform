# Packages

Shared contracts and reusable packages live here.

This folder is for cross-service contracts and shared implementation utilities that are explicitly allowed by the architecture. Keep domain ownership inside the owning service.
